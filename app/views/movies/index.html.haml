%h2 All Movies

%table#movies
  %thead
    %tr
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
      %th Indice

  %tbody
    - @moviesort = @movies.sort_by{|movie| movie.title.upcase}
    - @moviesort.each_with_index do |movie,index|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)
        %td= index

= link_to 'Add new movie', new_movie_path

